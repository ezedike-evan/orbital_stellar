{
  "name": "orbit-stellar",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" run build",
    "test": "pnpm -r --if-present test"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
